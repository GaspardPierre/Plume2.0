const memberModel = require('./member');
const workModel = require('./work');
const labelModel = require('./label');
const loginModel = require('./login');

module.exports = {  memberModel, workModel, labelModel, loginModel };